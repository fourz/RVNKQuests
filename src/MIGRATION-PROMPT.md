# Migration prompt — moved

The RVNKQuests migration prompt lives in the parent repo:

```
docs/migration/rvnkquests-migration-prompt.md
```

Related, same directory: `to-rvnkcore.md` (the general RVNKCore migration guide) and
`rvnkworlds-api-migration.md`.

> This file replaces a symlink to `C:/tools/_PROJECTS/Ravenkaft Dev/docs/migration/...`, broken
> since the move off Windows. **This project does not use symlinks** — they break silently on a
> cross-platform checkout and are invisible in a diff. Use a short text pointer instead.
